import React from 'react'

/* create a react component with its own custom name, custom features, and custom behavior */
function MyAmazingComponent() {
  return (
    /* JSX syntax, not html */
    <div> 
      <h1 className = "section-title section-title--blue">This is my amazing react component. </h1>
      <p className = "section-title section-title--blue">React is great.</p>
    </div>
  )
}

export default MyAmazingComponent;